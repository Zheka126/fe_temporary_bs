export interface BookType {
  img: string;
  title: string;
  author: string;
  genres: string[];
  uploadedBy: string;
  publicationDate: string;
  language: string;
  description: string;
  availability: string;
}
